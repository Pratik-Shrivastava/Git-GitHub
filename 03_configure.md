## ⚙️ Git Configuration

Before using Git, it's important to configure your identity. This ensures your commits are properly attributed.
```bash
git config --global user.name "Your Name"  
git config --global user.email "your.email@example.com"
```
---

### 📌 Why Configure Git?

Git uses the **email address** to associate commits with user accounts on platforms like:
- GitHub
- GitLab
- Bitbucket

This allows:
- Proper attribution of changes
- Communication tools (like comments, mentions, notifications) to link back to the correct user

---

✅ *Use the `--global` flag to apply the configuration to all repositories on your system.*
